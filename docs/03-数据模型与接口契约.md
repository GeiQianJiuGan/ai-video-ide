# 数据模型与接口契约

> 配套：[01-技术栈与架构](01-技术栈与架构.md) · [02-功能开发文档](02-功能开发文档.md)

## 0. 通用约定

- 主键：字符串 ULID（`chr_01H...` 前缀区分类型），时间可排序、跨机器不冲突。
- 时间：UTC ISO8601 存储，前端本地化显示。
- 所有表含 `created_at / updated_at`；软删除仅用于 Character/Location/Prop（`deleted_at`），生成版本不软删。
- 文件路径：**相对工程根目录**，正斜杠。
- 枚举在 Python `StrEnum` 与 TS 联合类型两侧生成，单一来源为后端 OpenAPI。

---

## 1. 实体关系总览

```text
Project
 ├─ Character ─┬─ Appearance(自引用 parent) ─┬─ CharacterSheet ─ SheetVersion
 │             └─ (default_appearance_id)     └─ view_regions
 ├─ Location ── LocationVariant ── LocationReference(版本)
 ├─ Prop ── PropReference(版本)
 ├─ Story(1:1) ── Scene ─┬─ SceneCast(→Character, Appearance)
 │                       └─ Shot ─┬─ ShotCast(→Character, Appearance, state)
 │                                ├─ ShotProp
 │                                ├─ ShotDependency(→Shot, strength)
 │                                ├─ PromptVersion
 │                                ├─ ShotContextOverride
 │                                └─ GenerationVersion ── (files)
 ├─ Workflow ─┬─ WorkflowCapability
 │            └─ WorkflowBinding
 ├─ Job ── JobDependency
 ├─ Asset (统一登记表，指向具体文件)
 ├─ ImportSession ── Clip ── ClipVersion        (阶段二)
 └─ Timeline ─ Track ─ TimelineClip ─┬─ Transition
                                     ├─ ClipEffect
                                     └─ Subtitle / AudioClip
```

---

## 2. 表定义（SQLite / 阶段一）

### 2.1 项目与资产

```sql
CREATE TABLE project (
  id TEXT PRIMARY KEY, name TEXT NOT NULL, synopsis TEXT,
  cover_asset_id TEXT, style_preset TEXT,
  width INTEGER NOT NULL DEFAULT 1280, height INTEGER NOT NULL DEFAULT 720,
  fps REAL NOT NULL DEFAULT 24, aspect_ratio TEXT DEFAULT '16:9',
  default_video_workflow_id TEXT, default_image_workflow_id TEXT,
  default_prompt_style TEXT, negative_prompt TEXT,
  schema_version INTEGER NOT NULL,
  created_at TEXT, updated_at TEXT
);

CREATE TABLE asset (
  id TEXT PRIMARY KEY, project_id TEXT NOT NULL,
  type TEXT NOT NULL,          -- character_sheet|location_ref|prop_ref|video|frame|audio|lora|lut|upload|export
  owner_type TEXT, owner_id TEXT,
  file_path TEXT NOT NULL, file_hash TEXT, mime TEXT, bytes INTEGER,
  width INTEGER, height INTEGER, duration REAL, fps REAL,
  source TEXT NOT NULL,        -- generated|imported|uploaded
  thumb_path TEXT, proxy_path TEXT, tags TEXT,  -- tags: JSON array
  meta TEXT,                   -- JSON
  created_at TEXT, updated_at TEXT
);
CREATE INDEX idx_asset_owner ON asset(owner_type, owner_id);

CREATE TABLE asset_usage (           -- 反向引用，用于孤儿检测与跳转
  asset_id TEXT NOT NULL, ref_type TEXT NOT NULL, ref_id TEXT NOT NULL,
  role TEXT, PRIMARY KEY (asset_id, ref_type, ref_id, role)
);
```

### 2.2 角色体系

```sql
CREATE TABLE character (
  id TEXT PRIMARY KEY, project_id TEXT NOT NULL, name TEXT NOT NULL,
  age TEXT, gender TEXT, personality TEXT,
  appearance_description TEXT,
  persistent_traits TEXT,      -- JSON: {face:"", scar:"", eye_color:"", ...}
  background TEXT,
  default_appearance_id TEXT, deleted_at TEXT,
  created_at TEXT, updated_at TEXT
);

CREATE TABLE appearance (
  id TEXT PRIMARY KEY, character_id TEXT NOT NULL,
  name TEXT NOT NULL,                -- 默认形象 / 学生时期 / 警察时期 / 受伤状态
  parent_appearance_id TEXT,         -- 继承来源
  inherited_fields TEXT,             -- JSON array: ["face_identity","hair","body_shape","persistent_traits"]
  age TEXT, costume TEXT, hairstyle TEXT, accessories TEXT,
  body_state TEXT, special_state TEXT, description TEXT,
  lora_asset_id TEXT, lora_trigger TEXT, lora_weight REAL,
  current_sheet_version_id TEXT,
  created_at TEXT, updated_at TEXT
);

CREATE TABLE sheet_version (
  id TEXT PRIMARY KEY, appearance_id TEXT NOT NULL, version INTEGER NOT NULL,
  asset_id TEXT NOT NULL,            -- 多视角整图
  view_regions TEXT NOT NULL,        -- JSON: {"front":[x,y,w,h],"three_quarter":[...],"side":[...],"back":[...],"full_body":[...],"half_body":[...]}  归一化 0-1
  description TEXT, prompt TEXT, negative_prompt TEXT,
  workflow_id TEXT, model TEXT, seed INTEGER, params TEXT,
  created_at TEXT,
  UNIQUE(appearance_id, version)
);
```

### 2.3 场景与道具

```sql
CREATE TABLE location (
  id TEXT PRIMARY KEY, project_id TEXT NOT NULL, name TEXT NOT NULL,
  description TEXT, architecture TEXT, environment TEXT,
  style TEXT, deleted_at TEXT, created_at TEXT, updated_at TEXT
);

CREATE TABLE location_variant (
  id TEXT PRIMARY KEY, location_id TEXT NOT NULL, name TEXT NOT NULL,  -- Day/Night/Rain/Snow/自定义
  parent_variant_id TEXT, inherited_fields TEXT,
  time_of_day TEXT, weather TEXT, lighting TEXT, notes TEXT,
  current_reference_id TEXT, created_at TEXT, updated_at TEXT
);

CREATE TABLE location_reference (
  id TEXT PRIMARY KEY, variant_id TEXT NOT NULL, version INTEGER NOT NULL,
  asset_id TEXT NOT NULL, camera_view TEXT,   -- wide|medium|detail|custom
  prompt TEXT, workflow_id TEXT, model TEXT, seed INTEGER, params TEXT,
  created_at TEXT, UNIQUE(variant_id, version, camera_view)
);

CREATE TABLE prop (
  id TEXT PRIMARY KEY, project_id TEXT NOT NULL, name TEXT NOT NULL,
  description TEXT, current_reference_asset_id TEXT,
  deleted_at TEXT, created_at TEXT, updated_at TEXT
);
```

### 2.4 故事 / Scene / Shot

```sql
CREATE TABLE story (
  id TEXT PRIMARY KEY, project_id TEXT NOT NULL,
  raw_script TEXT, logline TEXT, breakdown_meta TEXT, updated_at TEXT
);

CREATE TABLE scene (
  id TEXT PRIMARY KEY, project_id TEXT NOT NULL,
  order_index INTEGER NOT NULL, title TEXT, description TEXT,
  location_id TEXT, location_variant_id TEXT,
  time_of_day TEXT, mood TEXT, style_override TEXT,
  workflow_override_id TEXT, created_at TEXT, updated_at TEXT
);

CREATE TABLE scene_cast (
  scene_id TEXT NOT NULL, character_id TEXT NOT NULL,
  appearance_id TEXT NOT NULL, note TEXT,
  PRIMARY KEY (scene_id, character_id)
);

CREATE TABLE shot (
  id TEXT PRIMARY KEY, scene_id TEXT NOT NULL, project_id TEXT NOT NULL,
  order_index INTEGER NOT NULL, code TEXT,        -- "02-03"
  description TEXT,                                -- Story Description
  duration REAL NOT NULL DEFAULT 5,
  camera_angle TEXT, camera_movement TEXT, camera_lens TEXT,
  shot_size TEXT,                                  -- close_up|medium|full|wide|back|insert
  location_variant_override_id TEXT,
  generation_mode TEXT NOT NULL DEFAULT 'auto',    -- auto|t2v|i2v|r2v|extend|v2v|start_end
  workflow_override_id TEXT,
  params TEXT,                                     -- JSON: {seed,steps,cfg,candidates,width,height}
  status TEXT NOT NULL DEFAULT 'draft',
  current_version_id TEXT,
  created_at TEXT, updated_at TEXT
);
CREATE INDEX idx_shot_scene ON shot(scene_id, order_index);

CREATE TABLE shot_cast (
  shot_id TEXT NOT NULL, character_id TEXT NOT NULL, appearance_id TEXT NOT NULL,
  state TEXT NOT NULL DEFAULT 'existing',          -- existing|entering|background
  prominence INTEGER DEFAULT 0,                    -- 排序权重，决定裁剪优先级
  PRIMARY KEY (shot_id, character_id)
);

CREATE TABLE shot_prop (
  shot_id TEXT NOT NULL, prop_id TEXT NOT NULL, PRIMARY KEY (shot_id, prop_id)
);

CREATE TABLE shot_dependency (
  shot_id TEXT NOT NULL, depends_on_shot_id TEXT NOT NULL,
  strength TEXT NOT NULL DEFAULT 'strict',         -- strict|optional
  kind TEXT NOT NULL DEFAULT 'last_frame',         -- last_frame|video_extend|style
  PRIMARY KEY (shot_id, depends_on_shot_id)
);

CREATE TABLE prompt_version (
  id TEXT PRIMARY KEY, shot_id TEXT NOT NULL, version INTEGER NOT NULL,
  user_action TEXT,                                -- 用户输入的 [Action] 原文，永不被改写
  compiled TEXT,                                   -- 编译后的最终 prompt
  layers TEXT,                                     -- JSON: 各层内容与开关
  negative TEXT, source TEXT,                      -- manual|compiled|llm_polished
  created_at TEXT, UNIQUE(shot_id, version)
);

CREATE TABLE shot_context_override (
  shot_id TEXT PRIMARY KEY,
  payload TEXT NOT NULL,   -- JSON: {remove:[key], add:[{...}], replace:{key:{...}}}
  updated_at TEXT
);
```

### 2.5 生成版本与任务

```sql
CREATE TABLE generation_version (
  id TEXT PRIMARY KEY, shot_id TEXT, clip_id TEXT,      -- 二者其一
  version INTEGER NOT NULL, label TEXT,
  job_id TEXT, workflow_id TEXT, workflow_snapshot TEXT, -- 完整 graph JSON（或指向 generations/.../graph.json）
  model TEXT, seed INTEGER, params TEXT,
  prompt_version_id TEXT, compiled_prompt TEXT, negative_prompt TEXT,
  resolved_context TEXT NOT NULL,                        -- ResolvedContext 快照 JSON
  output_asset_id TEXT, first_frame_asset_id TEXT, last_frame_asset_id TEXT,
  thumb_path TEXT, proxy_path TEXT,
  duration REAL, width INTEGER, height INTEGER, fps REAL,
  elapsed_ms INTEGER, created_at TEXT,
  UNIQUE(shot_id, version)
);

CREATE TABLE job (
  id TEXT PRIMARY KEY, project_id TEXT NOT NULL,
  type TEXT NOT NULL,          -- generate_video|generate_image|extract_frame|proxy|export|import_split|v2v
  target_type TEXT, target_id TEXT,                       -- shot|appearance|clip|timeline
  status TEXT NOT NULL DEFAULT 'draft',
  priority INTEGER NOT NULL DEFAULT 0,
  request TEXT NOT NULL,       -- GenerationRequest JSON
  workflow_id TEXT, comfy_prompt_id TEXT,
  progress REAL DEFAULT 0, node_progress TEXT,
  attempt INTEGER DEFAULT 0, max_attempt INTEGER DEFAULT 1,
  error_code TEXT, error_detail TEXT, stderr_path TEXT,
  result_version_id TEXT,
  queued_at TEXT, started_at TEXT, finished_at TEXT,
  created_at TEXT, updated_at TEXT
);
CREATE INDEX idx_job_status ON job(status, priority DESC, created_at);

CREATE TABLE job_dependency (
  job_id TEXT NOT NULL, depends_on_job_id TEXT NOT NULL,
  strength TEXT NOT NULL DEFAULT 'strict',
  PRIMARY KEY (job_id, depends_on_job_id)
);
```

### 2.6 Workflow

```sql
CREATE TABLE workflow (
  id TEXT PRIMARY KEY, project_id TEXT,      -- NULL = 全局内置
  name TEXT NOT NULL, kind TEXT NOT NULL,    -- video|image
  origin TEXT NOT NULL,                      -- builtin|imported
  graph TEXT NOT NULL,                       -- ComfyUI workflow_api JSON
  capabilities TEXT NOT NULL,                -- JSON array
  limits TEXT,                               -- JSON: {max_ref_images, max_duration, resolutions[]}
  bindings TEXT NOT NULL,                    -- JSON: {"prompt":{"node":"6","input":"text"}, "image_slot":[{...}], ...}
  validation TEXT, enabled INTEGER DEFAULT 1,
  last_used_at TEXT, created_at TEXT, updated_at TEXT
);
```

`capabilities` 取值集合：
`text, image, video, character_reference, scene_reference, previous_frame, start_end_frame, extend, lora, controlnet, output_video, output_image`

### 2.7 Timeline

```sql
CREATE TABLE timeline (
  id TEXT PRIMARY KEY, project_id TEXT NOT NULL, name TEXT,
  fps REAL, width INTEGER, height INTEGER, updated_at TEXT
);

CREATE TABLE track (
  id TEXT PRIMARY KEY, timeline_id TEXT NOT NULL,
  kind TEXT NOT NULL,        -- video|audio|subtitle
  order_index INTEGER NOT NULL, name TEXT,
  muted INTEGER DEFAULT 0, locked INTEGER DEFAULT 0, hidden INTEGER DEFAULT 0
);

CREATE TABLE timeline_clip (
  id TEXT PRIMARY KEY, track_id TEXT NOT NULL,
  source_type TEXT NOT NULL,     -- generation_version|asset|clip_version
  source_id TEXT NOT NULL,
  shot_id TEXT,                  -- 便于 Auto-Assemble 就地替换与版本同步提示
  scene_id TEXT,                 -- Scene 分组标记
  start REAL NOT NULL,           -- 时间线上起点（秒）
  in_point REAL NOT NULL DEFAULT 0, out_point REAL NOT NULL,
  speed REAL DEFAULT 1, volume REAL DEFAULT 1,
  transform TEXT,                -- JSON: {x,y,scale,rotation,opacity,crop}
  fade_in REAL DEFAULT 0, fade_out REAL DEFAULT 0,
  created_at TEXT, updated_at TEXT
);
CREATE INDEX idx_clip_track ON timeline_clip(track_id, start);

CREATE TABLE transition (
  id TEXT PRIMARY KEY, track_id TEXT NOT NULL,
  from_clip_id TEXT, to_clip_id TEXT,
  type TEXT NOT NULL, duration REAL NOT NULL, params TEXT
);

CREATE TABLE clip_effect (
  id TEXT PRIMARY KEY, clip_id TEXT NOT NULL,
  type TEXT NOT NULL, order_index INTEGER NOT NULL,
  enabled INTEGER DEFAULT 1, params TEXT
);

CREATE TABLE subtitle (
  id TEXT PRIMARY KEY, track_id TEXT NOT NULL,
  start REAL, end REAL, text TEXT, style TEXT
);
```

### 2.8 导入与统一 Clip（阶段二）

```sql
CREATE TABLE import_session (
  id TEXT PRIMARY KEY, project_id TEXT NOT NULL,
  source_asset_id TEXT NOT NULL, split_method TEXT, split_params TEXT,
  created_at TEXT
);

CREATE TABLE clip (
  id TEXT PRIMARY KEY, project_id TEXT NOT NULL, import_session_id TEXT,
  order_index INTEGER, source_type TEXT,   -- none|original_video|shot
  source_asset_id TEXT, source_in REAL, source_out REAL,
  operation TEXT,                          -- generate|ai_edit
  workflow_id TEXT, current_version_id TEXT,
  status TEXT, created_at TEXT
);
-- ClipVersion 复用 generation_version（clip_id 字段）
```

---

## 3. 核心中立契约

### 3.1 GenerationRequest（业务层唯一出口，与模型无关）

```json
{
  "request_id": "req_01H...",
  "kind": "video",
  "target": { "type": "shot", "id": "sht_01H..." },
  "prompt": "两人在咖啡馆争吵，张伟突然站起来",
  "negative_prompt": "blurry, extra fingers",
  "character_refs": [
    { "character_id": "chr_1", "appearance_id": "app_2", "sheet_version_id": "shv_3",
      "view": "full_body", "crop": [0.51, 0.02, 0.24, 0.96],
      "file": "cache/crops/shv_3_full_body.png", "state": "entering", "weight": 1.0 }
  ],
  "scene_refs": [
    { "location_id": "loc_1", "variant_id": "var_night", "reference_id": "lrf_1",
      "file": "assets/locations/loc_1/night/ref_v1.png", "weight": 0.8 }
  ],
  "prop_refs": [],
  "previous_frame": { "shot_id": "sht_06", "version_id": "gen_12",
                      "file": "generations/sht_06/gen_12/last_frame.png" },
  "previous_video": null,
  "end_frame": null,
  "source_video": null,
  "style_ref": null,
  "duration": 10,
  "resolution": "1280x720",
  "fps": 24,
  "seed": 123456,
  "candidates": 4,
  "forced_mode": null,
  "extra_params": { "steps": 30, "cfg": 5.0 }
}
```

**必需能力推导**（Workflow Resolver 输入）：
`character_refs 非空 → character_reference`；`scene_refs 非空 → scene_reference`；`previous_frame → previous_frame`（或 `image`）；`previous_video → extend`；`source_video → video`；`end_frame → start_end_frame`；`kind=video → output_video`。

### 3.2 ResolvedContext（Context Resolver 输出）

```json
{
  "shot_id": "sht_07",
  "items": [
    { "key": "char:chr_1", "kind": "character_reference", "label": "张伟 / 警察时期 / Sheet v3",
      "asset_id": "ast_9", "file": "cache/crops/shv_3_full_body.png",
      "view": "full_body", "crop": [0.51,0.02,0.24,0.96],
      "state": "entering", "priority": 90, "slot": "image_slot[1]",
      "included": true, "source": "auto" },
    { "key": "prev_frame", "kind": "previous_frame", "label": "Shot 06 Last Frame",
      "priority": 100, "slot": "image_slot[0]", "included": true, "source": "auto" },
    { "key": "loc:loc_1", "kind": "scene_reference", "label": "咖啡馆 / 夜景 / v1",
      "priority": 70, "included": false, "omit_reason": "workflow_capacity",
      "source": "auto" }
  ],
  "prompt_version_id": "prm_3",
  "resolved_mode": "i2v",
  "workflow_id": "wf_wan_i2v",
  "workflow_reason": "满足 previous_frame + character_reference，能力冗余最小",
  "issues": [
    { "level": "warning", "code": "CONTEXT_OMITTED",
      "message": "场景参考因当前 Workflow 仅支持 2 张参考图被省略" }
  ]
}
```

优先级基准：`previous_frame 100 / entering character 90 / start_end 85 / scene_reference 70 / existing character 60 / prop 40 / style 20`。

### 3.3 结构化错误

```json
{
  "code": "MISSING_CAPABILITY",
  "title": "当前 Workflow 不支持 Character Reference",
  "detail": "Wan T2V 未声明 character_reference 能力，无法注入「张伟 / 警察时期」。",
  "suggestions": [
    "切换到 Wan I2V 或 Wan R2V",
    "为当前 Workflow 补充 image 槽位绑定",
    "移除该角色参考，仅用 Prompt 生成"
  ],
  "related_ids": { "shot_id": "sht_07", "workflow_id": "wf_wan_t2v", "character_id": "chr_1" }
}
```

---

## 4. REST 接口清单（`/api/v1`）

### 项目
```text
GET    /projects                        列表（含最近打开）
POST   /projects                        新建（dir, name, 默认参数）
POST   /projects/open                   打开目录（校验/迁移 schema）
GET    /projects/{pid}                  详情
PATCH  /projects/{pid}                  更新设置
GET    /projects/{pid}/dashboard        统计聚合
```

### 角色
```text
GET|POST        /projects/{pid}/characters
GET|PATCH|DELETE /characters/{cid}
GET|POST        /characters/{cid}/appearances
GET|PATCH|DELETE /appearances/{aid}
POST            /appearances/{aid}/derive         从本形象派生（变更表单）
GET             /appearances/{aid}/resolved       返回继承合并后的完整字段
GET|POST        /appearances/{aid}/sheets         列表 / 发起生成（candidates=N）
PATCH           /sheets/{svid}                    改 view_regions / description
POST            /appearances/{aid}/current-sheet  设为当前版本
```

### 场景 / 道具
```text
GET|POST        /projects/{pid}/locations
GET|PATCH|DELETE /locations/{lid}
GET|POST        /locations/{lid}/variants
GET|POST        /variants/{vid}/references        列表 / 发起生成
POST            /variants/{vid}/current-reference
GET|POST        /projects/{pid}/props
```

### 故事 / Scene / Shot
```text
GET|PUT   /projects/{pid}/story
POST      /projects/{pid}/story/breakdown         AI 分析 → 返回建议（不落库）
POST      /projects/{pid}/story/breakdown/apply   审阅后按选择落库
GET|POST  /projects/{pid}/scenes
GET|PATCH|DELETE /scenes/{sid}
POST      /projects/{pid}/scenes/reorder
GET|PUT   /scenes/{sid}/cast
GET|POST  /scenes/{sid}/shots
GET|PATCH|DELETE /shots/{shid}
POST      /shots/{shid}/move                      跨 Scene / 改序
POST      /shots/{shid}/duplicate
GET|PUT   /shots/{shid}/cast                      含 state
GET|PUT   /shots/{shid}/dependencies
```

### Context / Prompt
```text
GET    /shots/{shid}/context                      触发 Resolver，返回 ResolvedContext
PUT    /shots/{shid}/context/override             手动增删改
DELETE /shots/{shid}/context/override             恢复自动
GET    /shots/{shid}/prompts                      版本列表
POST   /shots/{shid}/prompts/compile              编译预览（不落库）
POST   /shots/{shid}/prompts                      落一个新版本
POST   /shots/{shid}/prompts/polish               LLM 润色（可选）
```

### 生成 / 队列 / 版本
```text
POST   /shots/{shid}/generate                     入队（candidates, overrides）
POST   /scenes/{sid}/generate                     Scene 批量
POST   /projects/{pid}/generate                   全项目待生成
GET    /projects/{pid}/jobs                       队列（filter/status/page）
GET    /jobs/{jid}                                详情（含 graph/stderr）
POST   /jobs/{jid}/cancel | /retry | /priority
POST   /projects/{pid}/queue/pause | /resume
GET    /projects/{pid}/queue/graph                依赖图
GET    /shots/{shid}/versions
POST   /shots/{shid}/versions/{vid}/set-current
POST   /shots/{shid}/versions/{vid}/continue      以此版本尾帧继续生成下一 Shot
DELETE /versions/{vid}
```

### Workflow / Asset / Timeline / Export / Settings
```text
GET|POST        /workflows                        列表 / 导入（workflow_api.json）
GET|PATCH|DELETE /workflows/{wid}
POST            /workflows/{wid}/validate         节点绑定 + ComfyUI 节点存在性校验
POST            /workflows/resolve                传 GenerationRequest → 返回候选与打分
GET             /projects/{pid}/assets            过滤 / 分页
GET             /assets/{aid} | /assets/{aid}/usage | /assets/{aid}/file
POST            /projects/{pid}/assets/upload
GET             /projects/{pid}/assets/orphans
GET             /projects/{pid}/timeline
POST            /timeline/{tid}/commands          批量编辑命令（原子事务，返回新状态）
POST            /timeline/{tid}/auto-assemble
POST            /timeline/{tid}/export             预检 + 入队导出 Job
GET             /settings | PATCH /settings
POST            /settings/comfy/test | /settings/ffmpeg/test | /settings/llm/test
GET             /system/gpu
```

Timeline 采用**命令数组**而非细粒度 REST，保证多步编辑的原子性与撤销对齐：

```json
{ "commands": [
  { "op": "split", "clip_id": "tcl_1", "at": 12.5 },
  { "op": "trim",  "clip_id": "tcl_2", "in_point": 1.0, "ripple": true },
  { "op": "replace_version", "clip_id": "tcl_3", "version_id": "gen_88" }
] }
```

---

## 5. WebSocket 事件

单端点 `/api/v1/ws?project_id=...&token=...`，客户端按 `channel` 订阅。

```json
{ "channel": "job", "event": "job.progress",
  "payload": { "job_id": "job_1", "shot_id": "sht_07", "progress": 0.62,
               "node": "KSampler", "step": 19, "total_steps": 30, "eta_ms": 41000 } }
```

| event | 说明 |
|---|---|
| `job.queued` / `job.started` / `job.progress` / `job.completed` / `job.failed` / `job.cancelled` | Job 生命周期 |
| `queue.state` | 队列摘要（running/queued/waiting 数、是否暂停） |
| `shot.status_changed` | Shot 状态推导结果变更（含 `needs_review`） |
| `version.created` / `version.current_changed` | 版本变化，前端刷新 VersionRail 与 Timeline 徽标 |
| `asset.created` | 新资产（Sheet / 尾帧 / 代理） |
| `system.comfy_status` | ComfyUI 在线/离线、队列长度 |
| `system.gpu` | VRAM 占用、利用率（每 2s） |
| `error` | 结构化错误推送（进通知中心） |

约定：所有事件幂等、可丢失；前端在重连后必须调用对应 REST 做一次全量对齐（`/jobs`、`/shots`），不依赖事件流保证一致性。

---

## 6. 调度器行为规范

```text
tick():
  1. 认领：扫描 status=running 且本进程无内存句柄的 Job → 查 ComfyUI /history[prompt_id]
           命中 → 走 collector 完成；未命中且超时 → failed(COMFY_LOST)
  2. 就绪判定：status in (queued, waiting_upstream)
       strict 依赖 → 上游 job.status=completed 且 上游 shot.current_version_id 已确认
       optional 依赖 → 上游 job.status in (running, completed) 即可
  3. 排序：priority DESC, created_at ASC
  4. 派发：并发数 < worker_limit 时取队首 → resolve context → resolve workflow
           → adapter 构图 → POST /prompt → 记录 comfy_prompt_id → running
  5. 失败：分类错误码；可重试类（COMFY_OFFLINE / GPU_OOM）按 max_attempt 退避重排
```

- `worker_limit` 默认 1；用户可调，但 UI 需提示 VRAM 风险。
- 入队时用 Kahn 算法做环检测，失败返回 `DEPENDENCY_CYCLE` 及环路径。
- 队列暂停 = 不再派发新任务，正在跑的不中断。
- 任一 Job 完成后立即广播 `queue.state`，前端不轮询。

---

## 7. 生成产物落盘规范

```text
generations/<shot_id>/<version_id>/
├── request.json      GenerationRequest（含 ResolvedContext 快照）
├── graph.json        实际提交给 ComfyUI 的 prompt graph
├── result.json       ComfyUI 返回 + ffprobe 元数据 + 耗时
├── output.mp4        原始产物（不做二次编码）
├── first_frame.png   FFmpeg 抽首帧
├── last_frame.png    FFmpeg 抽尾帧（供下游继承）
├── thumb.jpg         320px 缩略图
└── proxy.mp4         720p / 密集关键帧 / 供预览与 Timeline
```

写入顺序：临时目录 → 校验完整（ffprobe 成功、时长 > 0）→ 原子改名 → 落库。任一步失败则清理临时目录并标 Job 失败，**不留半成品版本记录**。

---

## 8. 必须覆盖的单元测试

| 模块 | 测试点 |
|---|---|
| Appearance 继承 | 多级继承合并、字段覆写、断开继承、循环父引用拒绝 |
| Context Resolver | 三种 Character State 的引用策略；镜头类型→视角映射；预算裁剪顺序与 `omit_reason`；override 合并；issues 生成 |
| Prompt Compiler | 分层顺序稳定；`[Action]` 原文不被改写；层开关；无 LLM 可运行 |
| Workflow Resolver | 能力匹配、缺能力报错内容、打分排序、强制 mode 与能力冲突 |
| Workflow Adapter | 绑定映射正确、多 image 槽位顺序、缺绑定报错 |
| Scheduler | strict/optional 就绪判定、环检测、并发上限、崩溃后认领、重试退避 |
| Version | 永不覆盖；set-current 触发下游 needs_review；被 Timeline 引用时禁止删除 |
| Timeline 命令 | split/trim(ripple,roll)/move 的边界与吸附；命令原子性；撤销后状态等价 |
| Effects 参数映射 | 预览参数 ↔ FFmpeg filter 参数一致性（数值对照表） |
| Export | 预检问题清单、filter_complex 生成、取消后清理 |
